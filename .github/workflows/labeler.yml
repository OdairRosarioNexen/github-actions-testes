name: "Pull Request Labeler"

on: [pull_request]

jobs:
  label:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/labeler@v4
      with:
        repo-token: "${{ secrets.SECRET_ACTIONS_PORTIFOLIO_EXEMPLE }}"